def typedD():
    print("A")
    helicopter_ = helicopter_.replace("d", "")

def typedE():
    print("a")
def typedS():
    print("a")

def typedT():
    print("a")

def typedR():
    print("a")

def typedO():
    print("a")

def typedY():
    print("a")    



""" def typedA():
    letter_want_gone = "a"
    for line in helicopter_:
      helicopter_name = helicopter_
      index = 0
      x = 1
      while (line[index] != "a"):
        print(helicopter_name)
        
        helicopter_name = helicopter_name + line[index]
        if (index <= len(helicopter_name)):
          print("hi")
          break
        index = index + 1
        x += 1
        
        
      if (line[index] == "a"):
        for a in letter_want_gone:
          helicopter_name = helicopter_name.replace("a", '')
        print(enemylist)
        enemylist.pop(0)
        enemylist.append(helicopter_name)
        enemy.clear()
        enemy.write(helicopter_name, font=("Arial", 10, "bold"))
        print(helicopter_name)
        print(enemylist)
        if (helicopter_name == ""):
          enemy.ht()

def typedO():
  print(enemylist)

  letter_want_gone = "o"
  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "o"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "o"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("o", '')

      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedR():

  letter_want_gone = "r"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "r"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "r"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("r", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedN():

  letter_want_gone = "n"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "n"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "n"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("n", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedG():

  letter_want_gone = "g"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "g"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "g"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("g", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedE():

  letter_want_gone = "e"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "e"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "e"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("e", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()
def typedB():

  letter_want_gone = "b"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "b"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "b"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("b", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()     
def typedL():

  letter_want_gone = "l"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "l"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "l"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("l", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()
def typedU():

  letter_want_gone = "u"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "u"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "u"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("u", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()
def typedP():

  letter_want_gone = "p"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "p"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "p"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("p", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedI():

  letter_want_gone = "i"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "i"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "i"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("i", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()         

def typedK():

  letter_want_gone = "k"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "k"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "k"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("k", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedY():

  letter_want_gone = "y"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "y"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "y"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("y", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()

def typedD():

  letter_want_gone = "d"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "d"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "d"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("d", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()      
def typedT():

  letter_want_gone = "t"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "t"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "t"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("t", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()      

def typedS():

  letter_want_gone = "s"

  for line in helicopter_:
    helicopter_name = helicopter_
    index = 0
    x = 1
    while (line[index] != "s"):

      helicopter_name = helicopter_name + line[index]
      if (index <= len(helicopter_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "s"):
      for a in letter_want_gone:
        helicopter_name = helicopter_name.replace("s", '')
      enemylist.pop(0)
      enemylist.append(helicopter_name)
      enemy.clear()

      enemy.write(helicopter_name, font=("Arial", 10, "bold"))
      if (helicopter_name == ""):
        enemy.ht()       """
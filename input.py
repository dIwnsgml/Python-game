def typedA():
    letter_want_gone = "a"
    for line in enemy_:
      enemy_name = enemy_
      index = 0
      x = 1
      while (line[index] != "a"):
        print(enemy_name)
        
        enemy_name = enemy_name + line[index]
        if (index <= len(enemy_name)):
          print("hi")
          break
        index = index + 1
        x += 1
        
        
      if (line[index] == "a"):
        for a in letter_want_gone:
          enemy_name = enemy_name.replace("a", '')
        print(enemylist)
        enemylist.pop(0)
        enemylist.append(enemy_name)
        enemy.clear()
        enemy.write(enemy_name, font=("Arial", 10, "bold"))
        print(enemy_name)
        print(enemylist)
        if (enemy_name == ""):
          enemy.ht()

def typedO():
  print(enemylist)

  letter_want_gone = "o"
  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "o"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "o"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("o", '')

      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedR():

  letter_want_gone = "r"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "r"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "r"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("r", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedN():

  letter_want_gone = "n"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "n"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "n"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("n", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedG():

  letter_want_gone = "g"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "g"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "g"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("g", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedE():

  letter_want_gone = "e"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "e"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "e"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("e", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()
def typedB():

  letter_want_gone = "b"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "b"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "b"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("b", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()     
def typedL():

  letter_want_gone = "l"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "l"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "l"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("l", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()
def typedU():

  letter_want_gone = "u"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "u"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "u"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("u", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()
def typedP():

  letter_want_gone = "p"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "p"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "p"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("p", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedI():

  letter_want_gone = "i"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "i"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "i"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("i", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()         

def typedK():

  letter_want_gone = "k"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "k"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "k"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("k", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedY():

  letter_want_gone = "y"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "y"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "y"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("y", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()

def typedD():

  letter_want_gone = "d"

  for line in enemy_:
    enemy_name = enemy_
    index = 0
    x = 1
    while (line[index] != "d"):

      enemy_name = enemy_name + line[index]
      if (index <= len(enemy_name)):
        break
      index = index + 1

      x += 1
    if (line[index] == "d"):
      for a in letter_want_gone:
        enemy_name = enemy_name.replace("d", '')
      enemylist.pop(0)
      enemylist.append(enemy_name)
      enemy.clear()

      enemy.write(enemy_name, font=("Arial", 10, "bold"))
      if (enemy_name == ""):
        enemy.ht()      